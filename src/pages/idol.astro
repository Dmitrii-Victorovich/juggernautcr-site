<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Кумир — Juggernaut</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{
      --bg:#0f1320; --fg:#e9eef7; --muted:#a8b0c3;
      --card:linear-gradient(180deg,rgba(255,255,255,.06),rgba(255,255,255,.03));
      --stroke:rgba(255,255,255,.08); --br:16px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;
      color:var(--fg);
      background:var(--bg);
    }
    .container{max-width:820px;margin:0 auto;padding:0 20px}
    header.topbar{padding:20px 0}
    h1{margin:20px 0 16px;font-size:28px}
    p.lead{
      margin:0 0 18px;
      color:var(--muted);
      white-space:nowrap; /* держим тег и ссылку в одну линию */
    }

    /* ссылка на RoyaleAPI рядом с тегом */
    .lead .ext-link{
      margin-left:8px;
      color:#9cb3ff;
      text-decoration:none;
      border-bottom:1px dashed rgba(156,179,255,.4);
    }
    .lead .ext-link:hover{
      color:#cfe0ff;
      border-bottom-color:currentColor;
    }

    .grid{
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
      gap:14px;
    }
    .card{
      display:flex; align-items:center; gap:12px;
      padding:14px 16px; border-radius:14px;
      border:1px solid var(--stroke); background:var(--card);
      text-decoration:none; color:var(--fg);
      transition:transform .12s ease, background .12s ease;
    }
    .card:hover{transform:translateY(-2px);background:rgba(255,255,255,.08)}
    .icon{width:28px;height:28px;display:grid;place-items:center;border-radius:8px;background:#1e253a}
    .label{font-weight:600}
    .sub{color:var(--muted);font-size:13px}

    .btn{
      display:inline-block; color:var(--fg); text-decoration:none;
      border:1px solid rgba(255,255,255,.2);
      padding:10px 16px; border-radius:999px;
      background:rgba(255,255,255,.06);
      transition:background .12s ease, transform .12s ease;
    }
    .btn:hover{background:rgba(255,255,255,.12);transform:translateY(-1px)}

    /* блок описания */
    .idol-info{
      margin-top:28px;
      font-size:16px;
      line-height:1.7;
      color:var(--muted);
    }
    .idol-info strong{color:var(--fg)}
  </style>
</head>
<body>
  <header class="topbar">
    <div class="container">
      <a class="btn" href="/">На главную</a>
    </div>
  </header>

  <main class="container">
    <h1>Кумир Создателя</h1>

    <p class="lead">
      Faith #CUQGPCJR0
      <a class="ext-link" href="https://royaleapi.com/player/CUQGPCJR0" target="_blank" rel="noopener">RoyaleAPI</a>
    </p>

    <div class="grid">
      <!-- Telegram -->
      <a class="card" href="https://t.me/mamochat" target="_blank" rel="noopener">
        <span class="icon" aria-hidden="true">
          <!-- Telegram SVG -->
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
            <path d="M21.9 3.1c.3.2.2.8-.1 1.9l-3.3 15.4c-.2.9-.7 1.1-1.4.7l-4-2.9-1.9 1.8c-.2.2-.4.3-.7.3l.2-4.1 7.5-6.8c.3-.3 0-.4-.4-.1L8 14.4 3.9 13c-.9-.3-.9-.9.2-1.3L21 2.9c.5-.2.8-.1.9.2Z" fill="currentColor"/>
          </svg>
        </span>
        <div>
          <div class="label">Telegram</div>
          <div class="sub">@mamochat</div>
        </div>
      </a>

      <!-- YouTube -->
      <a class="card" href="https://www.youtube.com/channel/UCWw203l1baIdLMtX-IxNEuQ" target="_blank" rel="noopener">
        <span class="icon" aria-hidden="true">
          <!-- YouTube SVG -->
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
            <path d="M23.5 6.2a3 3 0 0 0-2.1-2.1C19.2 3.5 12 3.5 12 3.5s-7.2 0-9.4.6A3 3 0 0 0 .5 6.2 31 31 0 0 0 0 12a31 31 0 0 0 .5 5.8 3 3 0 0 0 2.1 2.1c2.2.6 9.4.6 9.4.6s7.2 0 9.4-.6a3 3 0 0 0 2.1-2.1A31 31 0 0 0 24 12a31 31 0 0 0-.5-5.8Z" fill="currentColor"/>
            <path d="M9.75 15.02V8.98L15.5 12l-5.75 3.02Z" fill="#0f1320"/>
          </svg>
        </span>
        <div>
          <div class="label">YouTube</div>
          <div class="sub">Канал mamoyaan</div>
        </div>
      </a>

      <!-- Twitch -->
      <a class="card" href="https://www.twitch.tv/mamoyaan" target="_blank" rel="noopener">
        <span class="icon" aria-hidden="true">
          <!-- Twitch SVG -->
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
            <path d="M4 3h16v10.5l-4 4H12l-2.5 2.5H7.5V17H4V3Zm2 2v10h3.5v3l3-3h4l3-3V5H6Zm8 2h2v5h-2V7Zm-5 0h2v5H9V7Z" fill="currentColor"/>
          </svg>
        </span>
        <div>
          <div class="label">Twitch</div>
          <div class="sub">twitch.tv/mamoyaan</div>
        </div>
      </a>
    </div>

    <!-- блок с описанием кумира -->
    <div class="idol-info">
      <p>
        <strong>Мамоян</strong> - один из сильнейших игроков не только СНГ, но и всего мира.<br>
        Лучший киберспортивный стример в русскоязычном сообществе.<br>
        Регулярно занимает топовые позиции по итогам сезонов и принимает участие в <strong>CRL</strong>.<br>
        Вдохновляет тысячи игроков своим стилем игры и дружелюбной атмосферой на стримах.
      </p>
    </div>

  </main>
</body>
</html>
