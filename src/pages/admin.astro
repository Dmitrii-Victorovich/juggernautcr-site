<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Администрация — Juggernaut</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{
      --bg:#0f1320;
      --fg:#e9eef7;
      --muted:#a8b0c3;
      --card:linear-gradient(180deg,rgba(255,255,255,.06),rgba(255,255,255,.03));
      --stroke:rgba(255,255,255,.08);
      --gap:16px;
      --br:16px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;
      color:var(--fg);
      background:var(--bg);
    }
    main{max-width:980px;margin:40px auto;padding:0 20px}
    h1{margin:0 0 20px;font-size:28px}
    .grid{
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
      gap:var(--gap);
    }
    .card{
      background:var(--card);
      border:1px solid var(--stroke);
      border-radius:var(--br);
      padding:18px;
      text-align:center;
    }
    .avatar{
      width:96px;height:96px;
      border-radius:50%;
      object-fit:cover;
      margin:8px auto 12px;
      display:block;
      background:#1e253a; /* фон-заглушка */
    }
    .name{font-weight:600;margin-bottom:10px}
    .icons{display:flex;justify-content:center;gap:10px}
    .icon{inline-size:24px;block-size:24px;color:var(--fg);opacity:.8}
    .icon:hover{opacity:1}
    .back{
      display:inline-block;
      margin-top:20px;
      color:var(--fg);
      text-decoration:none;
      border:1px solid rgba(255,255,255,.2);
      padding:10px 16px;
      border-radius:999px;
      background:rgba(255,255,255,.06);
      transition:background .12s ease,transform .12s ease;
    }
    .back:hover{background:rgba(255,255,255,.12);transform:translateY(-1px)}
    .back:active{transform:translateY(0)}
    p{color:var(--muted)}
  </style>
</head>
<body>
  <main>
    <h1>Администрация по набору в кланы</h1>

    <div class="grid">
      <!-- Ирина -->
      <article class="card">
        <img class="avatar" src="/public/images/admin.png" alt="Ирина" />
        <div class="name">Ирина</div>
        <div class="icons">
          <a href="https://t.me/Jugger66" target="_blank" class="icon" aria-label="Telegram">
            <svg viewBox="0 0 24 24" width="24" height="24" fill="currentColor"><path d="M9.04 15.16 8.9 18.3c.33 0 .48-.14.65-.31l1.57-1.51 3.26 2.39c.6.33 1.03.16 1.2-.55l2.18-10.25c.19-.88-.33-1.22-.9-1l-12.8 4.94c-.87.34-.86.83-.15 1.05l3.28 1.02 7.61-4.8c.36-.22.7-.1.43.14"/></svg>
          </a>
        </div>
      </article>

      <!-- Максим -->
      <article class="card">
        <img class="avatar" src="" alt="Максим (потом)" />
        <div class="name">Максим</div>
        <div class="icons">
          <a href="https://t.me/MaKsOnB" target="_blank" class="icon" aria-label="Telegram">
            <svg viewBox="0 0 24 24" width="24" height="24" fill="currentColor"><path d="M9.04 15.16 8.9 18.3c.33 0 .48-.14.65-.31l1.57-1.51 3.26 2.39c.6.33 1.03.16 1.2-.55l2.18-10.25c.19-.88-.33-1.22-.9-1l-12.8 4.94c-.87.34-.86.83-.15 1.05l3.28 1.02 7.61-4.8c.36-.22.7-.1.43.14"/></svg>
          </a>
        </div>
      </article>

      <!-- Игорь -->
      <article class="card">
        <img class="avatar" src="" alt="Игорь (потом)" />
        <div class="name">Игорь</div>
        <div class="icons">
          <a href="https://t.me/IG_STR" target="_blank" class="icon" aria-label="Telegram">
            <svg viewBox="0 0 24 24" width="24" height="24" fill="currentColor"><path d="M9.04 15.16 8.9 18.3c.33 0 .48-.14.65-.31l1.57-1.51 3.26 2.39c.6.33 1.03.16 1.2-.55l2.18-10.25c.19-.88-.33-1.22-.9-1l-12.8 4.94c-.87.34-.86.83-.15 1.05l3.28 1.02 7.61-4.8c.36-.22.7-.1.43.14"/></svg>
          </a>
        </div>
      </article>
    </div>

    <p style="margin-top:20px;opacity:.8;">
      Важное примечание! Игорь занимается набором в клан у кого королевская башня 14 уровня и ниже
    </p>

    <a class="back" href="/">На главную</a>
  </main>
</body>
</html>
