<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Создатель — Juggernaut</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{
      --bg:#0f1320; --fg:#e9eef7; --muted:#a8b0c3;
      --card:linear-gradient(180deg,rgba(255,255,255,.06),rgba(255,255,255,.03));
      --stroke:rgba(255,255,255,.08); --br:16px;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;color:var(--fg);background:var(--bg)}
    .container{max-width:820px;margin:0 auto;padding:0 20px}
    header.topbar{padding:20px 0}
    main{padding:0 20px}
    h1{margin:20px 0;font-size:28px}
    .wrap{
      display:flex;gap:20px;align-items:flex-start;flex-wrap:wrap;
      background:var(--card);border:1px solid var(--stroke);border-radius:var(--br);padding:20px;
    }
    .avatar{width:128px;height:128px;border-radius:50%;object-fit:cover;background:#1e253a}
    .bio{min-width:260px;flex:1}
    .bio p{margin:0 0 10px}
    .icons{display:flex;gap:20px;margin-top:10px;align-items:center}
    .icon-mail{
      display:inline-flex;align-items:center;gap:6px;padding:6px 12px;border:1px solid rgba(255,255,255,.2);
      border-radius:999px;background:rgba(255,255,255,.06);text-decoration:none;color:var(--fg);font-size:14px;
      transition:background .12s ease,transform .12s ease;
    }
    .icon-mail:hover{background:rgba(255,255,255,.12);transform:translateY(-1px)}
    .icon-mail img{width:20px;height:20px;display:block}
    .btn{
      display:inline-block;color:var(--fg);text-decoration:none;border:1px solid rgba(255,255,255,.2);
      padding:10px 16px;border-radius:999px;background:rgba(255,255,255,.06);
      transition:background .12s ease,transform .12s ease;
    }
    .btn:hover{background:rgba(255,255,255,.12);transform:translateY(-1px)}
    .btn:active{transform:translateY(0)}
    .btn-row{display:flex;gap:12px;flex-wrap:wrap;margin-top:16px}
  </style>
</head>
<body>
  <!-- ВЫРОВНЕННАЯ ВЕРХНЯЯ КНОПКА -->
  <header class="topbar">
    <div class="container">
      <a href="/" class="btn">На Родину</a>
    </div>
  </header>

  <main class="container">
    <h1>По пустякам не беспокоить</h1>

    <section class="wrap">
      <video class="avatar" autoplay muted loop playsinline preload="metadata" poster="/public/images/creator-poster.jpg">
        <source src="/public/images/creator.mp4" type="video/mp4" />
      </video>

      <div class="bio">
        <p>Короткая биография — длинный пенсл.</p>
        <p>Гений, нищий, плейгёрл, филантроп.</p>
        <br>
        <p style="margin:10px 0 6px;">Для коммерческих предложений обращаться:</p>
        <div class="icons">
          <a href="mailto:juggernautcr1@gmail.com" class="icon-mail"><img src="/public/images/gmail.png" alt="Gmail" /> Gmail</a>
          <a href="mailto:juggernautcr@mail.ru" class="icon-mail"><img src="/public/images/mailru.png" alt="Mail.ru" /> Mail.ru</a>
        </div>
      </div>
    </section>

    <!-- ОТЗЫВЫ + КУМИР -->
    <div class="btn-row">
      <a href="/idol" class="btn">⭐ Кумир</a>
    </div>
  </main>
</body>
</html>
