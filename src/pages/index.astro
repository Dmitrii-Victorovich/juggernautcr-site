<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <title>Juggernaut CR</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <style>
      :root { --fg:#0b0b0c; --muted:#666; --ring:#9aa4ff; }
      * { box-sizing: border-box }
      body{ margin:0; font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; color:var(--fg) }
      header{ padding:64px 24px; text-align:center }
      h1{ font-size:clamp(32px,6vw,56px); margin:0 0 12px }
      p.lead{ margin:0 auto; max-width:820px; opacity:.9 }
      nav.cta{ display:flex; gap:12px; justify-content:center; margin:20px 0 0 }
      a.btn{ padding:10px 16px; border:1px solid currentColor; border-radius:12px;
             text-decoration:none; transition:.15s transform, .15s box-shadow }
      a.btn:focus-visible{ outline:none; box-shadow:0 0 0 4px var(--ring); transform:translateY(-1px)}
      footer{ padding:28px 24px 40px; display:flex; gap:12px; justify-content:center }
      @media (max-width:520px){ footer{ flex-direction:column; align-items:center } }
    </style>
  </head>
  <body>
    <header>
      <h1>Juggernaut CR</h1>
      <p class="lead">
        Добро пожаловать!
      </p>

      <nav class="cta">
        <a class="btn" href="/about">О проекте</a>
        <a class="btn" href="/gallery">Галерея</a>
      </nav>
    </header>

    <footer>
      <a class="btn" href="/admin">Администрация</a>
      <a class="btn" href="/creator">Создатель</a>
    </footer>

    <div style="text-align:center; color:var(--muted); padding-bottom:24px">© {year} Juggernaut CR</div>
  </body>
</html>
